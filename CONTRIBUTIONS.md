# Team Contributions

| Member                | Role                | Modules / Areas Owned                                             | Percent |
| --------------------- | ------------------- | ----------------------------------------------------------------- | ------- |
| Hung Phuc Chu (lead)  | End-to-end          | All `src/`, `web/`, `tests/`, `docs/`, `grading/`, `scripts/`     | 100%    |

> Replace this single-person row with one row per teammate before submission.
> Each member should appear with role, modules owned, and percent contribution.
> Total must sum to 100 %. Each member must have commits across at least two
> of: `src/`, `tests/`, `docs/`.

## Verification

Run `git shortlog -sne --all --no-merges` and write the output to
`reports/git_contributions.txt`. The TA compares this distribution to the
table above; each member's commit share must be within ±15 percentage points
of declared share.

```bash
git shortlog -sne --all --no-merges > reports/git_contributions.txt
```
