# Runtime dependencies — pinned per the Reproducibility Manifest rubric.
# Mirrored in pyproject.toml [project.dependencies] for pip-audit compatibility.

# Web framework
fastapi==0.124.3
starlette==0.49.1
uvicorn[standard]==0.30.0

# Configuration / validation
pydantic==2.7.4
pydantic-settings==2.3.4

# Pipeline orchestration
langgraph==1.0.10

# Graph store
neo4j==5.22.0

# LLM client (OpenAI-compatible endpoints, used for extraction + query)
openai==1.40.0
# OpenAI 1.40.0 is not compatible with httpx 0.28.x for this code path;
# pin the last 0.27 release so live query/load-test requests do not crash.
httpx==0.27.2

# Test / lint / load tooling — installed in the same image so `make test`,
# `make lint`, `make loadtest`, `pip-audit` all work inside the container.
pytest==9.0.3
pytest-cov==5.0.0
ruff==0.5.0
black==26.3.1
mypy==1.10.0
locust==2.29.0
pip-audit==2.7.3

# Build / packaging tools. Pin patched versions so `pip-audit` on a clean
# checkout does not flag vulnerabilities from the base image toolchain.
pip==26.1.1
setuptools==78.1.1
wheel==0.46.2
